/**
 * Sello de versión visible en la app.
 * Sirve para confirmar QUÉ build estás viendo en producción.
 * Súbelo cada vez que despliegues cambios grandes.
 */
export const APP_VERSION = 'v7.147'
export const APP_BUILD = '2026-07-22' // v7.145: el Resumen de /admin ya NO cuenta ventas de prueba - se detectan por el id de sesion de Stripe (cs_test_ = prueba, cs_live_ = real) o por la bandera sales.is_test (migracion 060); nota clara en la UI, interruptor para incluirlas y accion por tienda para marcar/desmarcar sus ventas como prueba (sin borrar nada). // v7.144: CONSOLA ADMIN v2 - detalle de negocio en panel lateral (logo, dueno, plan, prueba, registro, Stripe si/no, empleados, productos, ventas del mes, pedidos online, ultima venta) con las mismas acciones; filtros por plan y estado; columnas Stripe y Ultima venta. Sin migraciones nuevas. // v7.143: COTIZACIÓN PÚBLICA a nombre de la TIENDA — getPublicQuote pedía stores.phone/email (columnas inexistentes: la tabla tiene whatsapp), el select fallaba y la cotización salía a nombre de "Mercanta Business" con el logo vacío; ahora usa select('*') y muestra la tienda real. Pie: "Cotización emitida por [tienda]" + contacto (WhatsApp/correo) y Mercanta solo como "Generada con Mercanta Business" en gris chico. Mismo criterio en el PDF. // v7.142: CONSOLA DE ADMIN DE PLATAFORMA en /admin (solo súper-admin: profiles.is_platform_admin de la migración 059 o PLATFORM_ADMIN_EMAIL). Resumen (negocios, usuarios, planes, pruebas activas, comisión estimada), lista de negocios con cambio de plan/prueba/suspender, lista de usuarios con búsqueda. No toca el panel de las tiendas. // v7.141: MARKETING BETA — banner "Versión Beta" en el dashboard y en los footers; promo "Prueba gratuita de 5 días" en Suscripción (+ aviso de prueba Beta si el plan es Gratis); hero de la landing rediseñado colorido con badge "Beta · 5 días de prueba gratis", título de la promo y botón "Comenzar prueba gratis". // v7.140: (1) contraseña de empleado GUARDADA (migr. 058 login_password) con ojito + Actualizar; (2) empleados cobran con la cuenta Stripe del JEFE; (3) sin "Empleados" en el menú de empleados/supervisores; (4) empleados de planes pagos exportan el corte del DÍA; (5) Pedidos Online visible para el empleado receptor (lee la tienda del jefe con service-role); (6) recepción de pedidos: error real si falla. // v7.139: LEGIBILIDAD — AdminLayout forzaba texto casi blanco (#f8fafc) en TODO el contenido cuando el fondo del panel era oscuro: sobre tarjetas/inputs blancos quedaba INVISIBLE (inventario, formularios, botones Editar/Guardar). Ya no se hereda color; el contenido va sobre superficie clara. Input y Button (outline/ghost) ahora tienen color propio. EMPLEADOS: getEmployeeMeta y saveEmployeeMetaAction resisten columnas faltantes (reintento con columnas básicas) — antes el SELECT/UPSERT completo fallaba y no se veía ni guardaba nada. // v7.138: (1) EMPLEADOS — saneado de valores antes del upsert: los "" de inputs vacíos rompían salary (NUMERIC) y hire_date (DATE) y Postgres rechazaba TODO el guardado; ahora ''→null y se guarda de verdad. Diagnóstico _diag visible en la consola del NAVEGADOR. (2) INVENTARIO — columna Estado ya no se oculta en móvil, badges con colores explícitos (Activo/Stock bajo/Agotado/Inactivo) y precio en negrita. (3) SUSCRIPCIÓN — botón "Reportar problema" con formulario simple que guarda en la tabla reports (order_no='APP'). // v7.137: (1) EMPLEADOS — getEmployeeMeta SIEMPRE devuelve objeto completo y crea la fila si falta; saveEmployeeMetaAction con upsert onConflict:'employee_id' (antes chocaba con la fila sembrada y no persistía) + verificación leyendo de vuelta + logs "Meta cargada". (2) STRIPE — la comprobación de la cuenta conectada YA NO BLOQUEA el cobro (si no es usable se cobra a la plataforma) y hay reintento sin destino si Stripe lo rechaza; el webhook ahora registra SIEMPRE las ventas del POS (source=pos) aunque los items no quepan en metadata. // v7.136: EMPLEADOS definitivo — la app NUNCA usa una tabla "employees" (usa profiles+employee_meta); el error 'relation employees does not exist' venía de SQL externo. Migración 057 (idempotente): (re)crea employee_meta + employee_attendance + funciones my_employee_ids/list_my_employees + RLS, SIEMBRA una fila de meta vacía por empleado (el modal siempre carga), y crea una VISTA de compatibilidad "employees". Código: bossOwnsEmployee ahora verifica por profiles.boss_id (fiable) — guardar meta/foto funciona aunque el RPC esté roto. Más logs. // Historial en git.
