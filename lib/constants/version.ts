/**
 * Sello de versión visible en la app.
 * Sirve para confirmar QUÉ build estás viendo en producción.
 * Súbelo cada vez que despliegues cambios grandes.
 */
export const APP_VERSION = 'v7.64'
export const APP_BUILD = '2026-06-25' // v7.64: Nómina: tfoot solo Neto+ (compacta, no tapa); Pago base, Bonos, ISR/Seguro, Neto, Totales en text-sm font-bold. Colores: useEffect sync de primary/panel/bg/dashboard en StoreSettingsForm para que guarden y se vean en refresh (AdminLayout + catalog). Build limpio.
